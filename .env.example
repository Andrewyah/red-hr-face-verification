# Generate each independently: python -c "import secrets,base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
# Secrets are backend-only. Never commit real values or prefix them VITE_/NEXT_PUBLIC_.
FACE_SERVICE_HMAC_KEY=
FACE_TEMPLATE_KEY=
PORT=8080
